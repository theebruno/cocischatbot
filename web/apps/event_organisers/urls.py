"""
"""
from django.urls import path, re_path
from apps.departments import views

urlpatterns = [
    # # event_organisers
    # path('', views.index, name='event_organisers'),
    #
    # # # Matches any html file
    # # re_path(r'^.*\.*', views.pages, name='pages'),
]
